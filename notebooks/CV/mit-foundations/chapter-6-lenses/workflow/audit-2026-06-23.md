# Chapter 6 (Lenses) — Style & Cleanup Audit

**Date:** 2026-06-23
**Target:** `notebooks/CV/mit-foundations/chapter-6-lenses/index.ipynb` (17 cells)
**Scope:** read-only audit. No cells modified, notebook not executed.
**Hard constraint for any later cleanup:** figures 6.3(a), 6.4(a), 6.4(b) must stay **byte-identical** in `images/` after each edit; revert anything that changes a rendered PNG.

---

## 1. Cell inventory

| # | Type | Lines | `hide-input` | Helpers used (grep-confirmed) |
|---|------|------:|:---:|---|
| 0 | markdown | 11 | — | — (chapter intro) |
| 1 | code | 21 | ✗ | — (imports / `IMAGES_DIR`) |
| 2 | markdown | 22 | — | — (§6.2 intro + notation table) |
| 3 | code | 105 | ✗ | **physics-helpers cell** (defines `snells_law`, `paraxial_snell`, `surface_angle`, `focal_length`, `image_distance`, `lensmaker_angle_bookkeeping`) |
| 4 | markdown | 1 | — | — (paraxial approximation) |
| 5 | markdown | 15 | — | — (lensmaker derivation) |
| 6 | markdown | 1 | — | — (Fig 6.3(a) caption) |
| 7 | code | 184 | **✗ MISSING** | `snells_law`, `draw_angle` ×2 |
| 8 | markdown | 1 | — | — (Fig 6.4(a) caption) |
| 9 | code | 286 | **✗ MISSING** | `focal_length`, `image_distance` |
| 10 | markdown | 1 | — | — (thin-lens idealization) |
| 11 | markdown | 0 | — | — (**empty cell**) |
| 12 | markdown | 1 | — | — (Fig 6.4(b) caption) |
| 13 | code | 112 | ✓ | **drawing-helpers cell** (defines `draw_angle`, `lens_curves`, `surface_x_at_y`, `draw_biconvex_lens`, `solve_surface_hit_from_ray`, `visually_exaggerate_angle`, `draw_surface_slice`) |
| 14 | markdown | 3 | — | — ("Reading Figure 6.4(b)") |
| 15 | code | 363 | ✓ | `lensmaker_angle_bookkeeping`, `lens_curves`, `surface_x_at_y`, `solve_surface_hit_from_ray`, `draw_biconvex_lens` |
| 16 | markdown | 13 | — | — (derivation to formula) |

**Inventory flags**

- **F-1 (tags drifted):** the two figure cells **7 (6.3a) and 9 (6.4a) have lost their `hide-input` tag** — only 13 and 15 carry it now. (This changed between edit sessions; the file has been re-saved from a buffer that dropped them.) Per the brief, every pure-plotting cell must be tagged.
- **F-2 (empty cell):** cell 11 is an empty markdown cell — dead structural noise between the thin-lens note (10) and the 6.4(b) caption (12).

---

## 2. Helper usage matrix

Rows = every helper defined; columns = the three figure code cells. Numbers are call-sites (calls, not defs).

| Helper (cell) | 6.3a (7) | 6.4a (9) | 6.4b (15) | Internal use | Verdict |
|---|:--:|:--:|:--:|---|---|
| `snells_law` (3) | 1 | 0 | 0 | sanity print | **used** |
| `paraxial_snell` (3) | 0 | 0 | 0 | called inside `lensmaker_angle_bookkeeping` ×2 + sanity print | **used (internal)** |
| `surface_angle` (3) | 0 | 0 | 0 | called inside `lensmaker_angle_bookkeeping` ×2 + sanity print | **used (internal)** |
| `focal_length` (3) | 0 | 1 | 0 | sanity print | **used** |
| `image_distance` (3) | 0 | 1 | 0 | sanity print | **used** |
| `lensmaker_angle_bookkeeping` (3) | 0 | 0 | 1 | — | **used** |
| `draw_angle` (13) | 2 | 0 | 0 | — | **used (6.3a only)** |
| `lens_curves` (13) | 0 | 0 | 1 | — | **used** |
| `surface_x_at_y` (13) | 0 | 0 | 1 | called inside `solve_surface_hit_from_ray` | **used** |
| `draw_biconvex_lens` (13) | 0 | 0 | 1 | — | **used** |
| `solve_surface_hit_from_ray` (13) | 0 | 0 | 1 | — | **used** |
| `visually_exaggerate_angle` (13) | 0 | 0 | 0 | none | **DEAD — remove candidate** |
| `draw_surface_slice` (13) | 0 | 0 | 0 | none | **DEAD — remove candidate** |

**Findings**

- **F-3 (dead helpers):** `visually_exaggerate_angle` and `draw_surface_slice` are defined but **never called anywhere**. Both were used only by the dropped supplementary angle-inset figure (`fig06_04b_angles.png`). Safe to delete from cell 13 — they cannot affect any rendered PNG.
- **F-4 (hand-rolled lens in 6.4a):** Fig 6.4(a) does **not** call `draw_biconvex_lens`/`lens_curves`. It builds its own lens outline from true circular arcs (`cx`, `R`, `x_half = cx + sqrt(R² − y²)`), white-filled, black-outlined. This is a **genuinely different shape** from the helper's `sqrt`-elliptical, blue-filled body. Refactoring to the helper **would change the image** → refactor, high risk (see §8 discuss list).
- **F-5 (angle-drawing inconsistency):** three different angle-arc styles across the chapter — 6.3(a) uses the `draw_angle` helper (plain arc), 6.4(a) hand-rolls an `ax.plot` arc, 6.4(b) hand-rolls `FancyArrowPatch` arc-arrows. Only 6.3(a) reuses the helper. Unifying is a refactor with pixel-identity risk (different sample counts / arrowheads).

---

## 3. Code-bloat hotspots

### Cell 7 — Fig 6.3(a), 184 lines
- **Symmetric duplication:** two near-identical `draw_angle(...)` calls (θ₁, θ₂) and two media-label `ax.text(...)` blocks. Already routed through the `draw_angle` helper — low bloat.
- **Boilerplate ≥3×:** `FancyArrowPatch` appears 2× (under threshold). `ax.plot` 4× (interface, normal, incident ray — distinct, not duplication).
- **Physics literals in drawing:** none — `theta2` is derived via `snells_law`; all drawing offsets are cosmetic. ✓
- **Verdict:** cleanest of the three. No action needed beyond the missing tag.

### Cell 9 — Fig 6.4(a), 286 lines (largest)
- **Largest near-duplicated block:** the **two distance brackets `a` and `b`** — each is 3× `ax.plot(... color=red, lw=1.3, zorder=4)` (span line + two end ticks) plus a label, ~30 lines each, structurally identical. Fig 6.4(b) **already collapses this exact pattern into a `for` loop** — 6.4(a) does not. Candidate for the same loop.
- **Second duplication:** the two angle markers (θ₁ object-side, θ₄ image-side) — ~20 lines each, near-identical arc-sample + label. Could route through `draw_angle` (as 6.3a does) — but the helper samples 80 points vs the cell's 60, so the arc would differ → image risk.
- **Boilerplate ≥3×:** the red bracket `ax.plot(...)` idiom appears **6×** — the clearest "could be a loop/helper" signal in the chapter.
- **Physics literals in drawing:** none stray — `obj`, `img`, `lens_hit`, `f`, `b`, `theta1`, `theta4` are all derived from the physics block. ✓
- **Verdict:** real reduction available via a bracket loop (medium risk — must verify byte-identity), but the lens-shape and angle-marker refactors are image-risky.

### Cell 15 — Fig 6.4(b), 363 lines (most complex)
- **Already loops** the symmetric work it can: red height brackets (C₁/C₂), lower brackets (a/b/d), and the six main angle arcs (`main_angle_specs`). Good.
- **Remaining symmetric duplication:** the **front-vs-back surface constructions** — normals (`front_normal_p0/p1`, `back_normal_p0/p1`), right-angle marks (`front_mark_a/b/c`, `back_mark_a/b/c`), and parallel-axis reference lines/labels — are pairwise duplicated, ~35 lines total. Could fold into a `for surface in [front, back]` loop. Medium risk (order-sensitive for pixel-identity).
- **Recurring idiom:** `center + length * torch.stack([torch.cos(a), torch.sin(a)])` appears **~10×** (normals, marks, angle-label placement). This is exactly the `point_from_angle` helper that was **previously removed** as a "trivial wrapper." Reintroducing it would cut the most lines but reverses an earlier decision — flag for discussion.
- **DEAD parameters:** `theta23_scale`, `theta23_target_min_deg`, `theta23_target_max_deg` are defined in the parameter block but **used 0 times** (they fed `visually_exaggerate_angle`/`theta2_display`, both gone with the angle-inset). Safe to delete — no image effect.
- **Physics literals in drawing:** none stray; geometry flows from `lensmaker_angle_bookkeeping`. Layout constants are correctly isolated in the "Visual layout knobs" section. ✓

---

## 4. Markdown style violations

Toolchain grep (`matplotlib|savefig|dpi|plt|install|pip|headless`): **no true hits.** (The regex flagged the English words "Figure"/"figure" and "renders" — see M-3 for the one real concern.)

| Cell | Issue | Quote | Suggested fix |
|---|---|---|---|
| 0 | **M-1 first person** | "**We** reproduce the chapter's diagrammatic figures…", "two of **our** thin-lens helpers" | "This tutorial reproduces…", "two of the thin-lens helpers" |
| 0 | **M-2 over-promises unbuilt content** | describes a "depth-of-field section" and "telescope" with Kornia blur | Notebook currently contains only §6.2 (figs 6.3a/6.4a/6.4b). Either build those sections or scope the intro to what exists. |
| 14 | **M-3 STALE — describes the dropped figure** | "renders **two coordinated views**… On the **right, an angle inset** zooms into each surface" | The cell now renders **one** labeled diagram; the angle-inset (`fig06_04b_angles.png`) was removed. Rewrite to describe the single figure. Also "renders" → "draws" to avoid toolchain tone. **Highest-value markdown fix.** |
| 2 | M-1 first person | "**We** derive it by applying Snell's law twice", "**we** disambiguate by context" | "The derivation applies Snell's law twice", "disambiguate by context" |
| 5 | M-1 first person | "the form **we** set out to derive" | "the form derived above" |
| 6 | M-1 first person | "**we** don't reproduce the photo here" | "the photo is not reproduced here" |
| 12 | M-1 first person | "To derive the lensmaker's formula **we** follow a *single* ray" | "The derivation follows a *single* ray" |
| 16 | M-1 first person | "**we** recover the **lensmaker's formula**" | "this recovers the lensmaker's formula" |

**Code-first vs concept-first (judgment call):** the figure-caption cells (6, 8, 12) open with the concept ("A ray crosses from a medium…") rather than what the next cell does. For *figure captions* this is conventional and arguably fine; the brief's "code-first" rule targets prose that introduces computational cells. Cell 14 already opens code-first ("The cell below renders…"). **Recommend: leave 6/8/12 as captions; low priority.**

**Dates/version language:** only "MIT Press, 2024" (a citation — keep). No "newly added", no version-of-the-day language. ✓

---

## 5. Inline-comment style violations

Grep of every `#` comment in all code cells for toolchain terms (`matplotlib|savefig|dpi|plt|install|pip|headless`) and AI/first-person voice (`we|I|let's|our|previously`): **zero hits across all 6 code cells.** Comments are consistently code-first, concept-second, and toolchain-free (e.g. "Compute the focal length from the lensmaker's formula, then the image distance…"). **No action.**

---

## 6. NumPy contamination

Grep `numpy | np. | import numpy` across the entire notebook: **zero hits.** ✓ Clean — PyTorch-only throughout.

---

## 7. File-structure check

- ✓ `index.ipynb` exists.
- ✓ `images/` exists with exactly the three expected PNGs: `fig06_03a.png` (24,863 B), `fig06_04a.png` (31,769 B), `fig06_04b.png` (83,277 B).
- ✓ **No stray files** — the dropped `fig06_04b_angles.png` and all `_vN.png` version snapshots are gone; no `*_test/_clean/_teaching/_temp` PNGs remain.
- ✓ All three `savefig` targets in the notebook match the three files on disk.

**File structure is clean.** (Establish the byte-baseline of these three PNGs before any cleanup edit so pixel-identity can be checked.)

---

## 8. Concrete recommendations (ordered by impact-to-risk)

### Tier A — fix now, **zero image risk** (metadata / dead code / prose only)

| ID | Action | Why | Risk |
|---|---|---|---|
| F-1 | Re-add `hide-input` tag to cells **7 and 9** | Brief requires all pure-plotting cells tagged; currently inconsistent | None (metadata) |
| M-3 | Rewrite cell 14 to describe the **single** figure (drop "two coordinated views / angle inset on the right"); "renders"→"draws" | Markdown describes a figure that no longer exists | None (markdown) |
| F-3 | Delete dead helpers `visually_exaggerate_angle`, `draw_surface_slice` from cell 13 | Never called anywhere | None (unused) |
| §3 | Delete dead params `theta23_scale`, `theta23_target_min_deg`, `theta23_target_max_deg` from cell 15 | Used 0× | None (unused) — verify render unchanged |
| M-1 | Convert first-person "we/our" → second-person/impersonal in cells 0, 2, 5, 6, 12, 16 | Brief: evergreen, reader-facing voice | None (markdown) |
| F-2 | Delete empty markdown cell 11 | Structural noise | None |
| M-2 | Scope cell 0 intro to built content (or note remaining sections are forthcoming) | Intro promises depth-of-field/telescope sections not present | None (markdown) |

> Even "zero image risk" code deletions (F-3, dead params) should be followed by a re-execute + byte-compare of the affected PNG, per the constraint — but they touch only unused names, so the image must not move.

### Tier B — **discuss before doing** (refactors that risk changing a rendered PNG)

| ID | Action | Payoff | Risk |
|---|---|---|---|
| F-4 | Route Fig 6.4(a) lens outline through `draw_biconvex_lens`/`lens_curves` | DRY, one lens routine | **High — different shape; image WILL change.** Only do if willing to re-bless 6.4(a). |
| §3-9 | Collapse Fig 6.4(a) `a`/`b` brackets into a loop (mirror 6.4b) | −~30 lines | Medium — same calls in same order *should* be byte-identical; must verify. |
| §3-15 | Collapse Fig 6.4(b) front/back surface blocks into a `for surface in [...]` loop | −~35 lines | Medium — order-sensitive; verify byte-identity. |
| E | Reintroduce a `point_from_angle` helper for the ~10× `center + len*stack([cos,sin])` idiom in 6.4(b) | Largest line reduction | Medium + **reverses an earlier removal decision** — needs sign-off. |
| F-5/B | Unify angle drawing (make 6.4a/6.4b use `draw_angle`) | Consistency | **High** — sample-count/arrowhead differences change pixels. |

**Suggested order:** do all of Tier A in one pass (verify the three PNGs stay byte-identical after the dead-code deletions; markdown/tag edits can't move pixels). Then, only if you want the line savings, take Tier B items **one at a time**, re-rendering and byte-comparing after each — starting with the lowest-risk bracket loop (6.4a) and stopping at the first one that changes a PNG.

---

### Most actionable, at a glance
1. **Re-tag cells 7 & 9 `hide-input`** (regression; trivial).
2. **Fix stale cell 14 markdown** (describes a deleted figure).
3. **Delete dead code:** 2 helpers (`visually_exaggerate_angle`, `draw_surface_slice`) + 3 unused `theta23_*` params.
4. **De-"we" the markdown** (6 cells).
5. Everything that actually reduces drawing-code length (lens helper, bracket/surface loops) is **image-risky → Tier B, discuss first.**
